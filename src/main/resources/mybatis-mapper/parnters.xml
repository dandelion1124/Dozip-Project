<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="partners">
    <!--파트너스 정보 조회 -->
    <select id="partners_info" resultType="PartnersVO">
        select * from partnersT where pId=#{pId}
    </select>

    <!--파트너스 회원가입 -->
    <insert id="partners_in">
        insert into partnersT (businessNum, businessName, pId, pPw, pName, pTel, pMailId, pMailDomain, pDate)
        values(#{businessNum}, #{businessName}, #{pId}, #{pPw}, #{pName}, #{pTel}, #{pMailId}, #{pMailDomain}, sysdate)
    </insert>

    <!-- 파트너스 아이디 찾기위한 정보조회 -->
    <select id="findinfo_id" resultType="PartnersVO">
        select * from partnersT where businessNum=#{businessNum}
    </select>


    

</mapper>

