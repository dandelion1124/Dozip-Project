<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="partners">
    <select id="partners_info" resultType="PartnersVO">
        select * from partnersT where pId=#{pId}
    </select>

    <select id="data_manage" resultType="PartnersVO">
        <!--select * from partnersT where business_num=#{business_num} -->
        select * from partnersT where business_num=105
    </select>
</mapper>

